spring:
  application:
    name: gateway-service
  config:
    import: "configserver:http://localhost:9999"

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health, info, gateway