spring:
  application:
    name: auth-service
  cloud:
    config:
      uri: ${SPRING_CLOUD_CONFIG_URI:http://localhost:9999}
      fail-fast: true
      retry:
        initial-interval: 3000
        max-interval: 10000
        max-attempts: 20