spring:
  application:
    name: analysis-service
  config:
    import: "configserver:http://localhost:9999"

server:
  port: 8082

management:
  endpoints:
    web:
      exposure:
        include: health, info