spring:
  application:
    name: location-service
  config:
    import: "configserver:${SPRING_CLOUD_CONFIG_URI:http://localhost:9999}"

server:
  port: 8082

management:
  endpoints:
    web:
      exposure:
        include: health, info